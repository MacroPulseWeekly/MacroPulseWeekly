name: Update MacroPulseWeekly Charts

permissions:
  contents: write

on:
  schedule:
    # Runs every 6 hours
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          # Ensure this matches your actual script filename
          cache-dependency-path: 'generate_charts.py'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install plotly yfinance pandas fredapi lxml beautifulsoup4 requests google-search-results

      - name: Run chart generator
        env:
          # Mapping your GitHub Secrets to the Python Environment
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
          SERPAPI_KEY: ${{ secrets.SERPAPI_KEY }}
          FMP_API_KEY: ${{ secrets.FMP_API_KEY }}
        run: python generate_charts.py

      - name: Commit and push updated charts & index
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # 1. Stage the changes FIRST so Git knows we want to keep them
          git add charts/ index.html
          
          # 2. Pull with rebase and autostash to avoid the "exit code 128"
          git pull origin main --rebase --autostash
          
          # 3. Only commit if there are actual differences
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Automated macro update [$(date +'%Y-%m-%d %H:%M')]"
            git push origin HEAD:main
          fi
